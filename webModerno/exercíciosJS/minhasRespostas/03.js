// Crie uma função que recebe dois parâmetros, base e expoente, e retorne a base elevada ao expoente.
function expo(a, b) {
  return a ** b
};

console.log(expo(2, 4));

// outra maneira de fazer com arrow function!!
calculo = (a, b) => a ** b;

console.log(calculo(2, 3));